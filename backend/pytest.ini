[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
addopts = --import-mode=importlib
markers =
	legacy: covers deprecated pre-V05 content APIs and loaders.
	v05: covers the new V05 architecture and content systems.
	gold: covers production-ready, high-integrity V05 features.
