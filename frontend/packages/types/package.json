{
    "name": "@rpg/types",
    "version": "0.0.1",
    "private": true,
    "main": "./index.ts",
    "types": "./index.ts",
    "scripts": {
        "generate": "json2ts -i 'schemas/*.json' -o index.ts --cwd schemas",
        "build": "tsc",
        "test": "echo \"No tests for types package\""
    },
    "devDependencies": {
        "json-schema-to-typescript": "^13.0.0",
        "typescript": "^5.0.0"
    }
}